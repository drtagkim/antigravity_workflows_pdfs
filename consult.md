---
description: PDF 인덱스 데이터를 바탕으로 사용자의 질문과 아이디어를 분석하고 컨설팅 결과를 마크다운으로 정리하는 워크플로우
---

# 데이터 컨설팅 및 인사이트 도출 (consult)

이 워크플로우는 `index-pdf-data` 워크플로우를 통해 생성된 `index.json` 데이터를 바탕으로 동작합니다. 사용자의 질문과 아이디어를 논리적으로 해석하여 의견을 제시하고, 그 컨설팅 결과를 폴더명과 동일한 이름의 체계적인 마크다운 문서로 기록합니다.

## 1. 데이터 로드 및 초기 컨텍스트 구성
- **대상 폴더 지정**: 사용자가 컨설팅을 진행할 데이터 폴더(예: `주요추진내용`)를 지정합니다.
- **index.json 스캔**: 지정된 폴더 내의 `index.json` 파일을 읽어들여 추출된 텍스트, 요약, 이미지 구조 등 모든 데이터를 AI의 컨텍스트(Context) 메모리에 로드합니다.

## 2. 사용자 질의응답 및 컨설팅 (Consulting)
- **아이디어 해석 및 분석**: 사용자가 데이터를 기반으로 질문을 던지거나 아이디어를 제시하면, AI는 `index.json`의 팩트 데이터를 근거로 논리적인 해석과 분석 의견을 실시간으로 답변합니다.
- **다각적 의견 제시**: 단순한 텍스트 요약을 넘어, 문서 간의 연관성, 누락된 논리, 향후 발전 방향 등 깊이 있는 인사이트를 제안합니다.

## 3. 컨설팅 결과 문서화 (Markdown Report)
- **문서 생성 및 파일명 규칙**: 컨설팅이 진행되는 동안 오간 핵심 질문, AI의 분석 답변, 그리고 도출된 결론을 **작업 폴더와 동일한 이름의 마크다운 파일**(예: `주요추진내용.md`)로 자동 정리합니다. (해당 파일은 작업 폴더 내에 생성됩니다)
- **문서 구조**:
  - **문서 개요**: 어떤 데이터(`index.json` 기준)를 바탕으로 진행된 컨설팅인지 명시합니다.
  - **Q&A 로그**: 사용자의 질문(Q)과 AI의 논리적 분석 및 답변(A)을 가독성 좋게 (인용구, 글머리 기호 등) 정리합니다.
    - **중요**: 마크다운 렌더링 이슈 방지를 위해 강조할 텍스트는 `**강조**` 대신 반드시 HTML 태그인 `<strong>강조</strong>` 형태를 사용합니다.
  - **인사이트 및 결론 (Insights/Conclusion)**: 논의된 내용을 바탕으로 한 핵심 결론과 제언을 요약합니다.

## 4. 지속적인 업데이트 (Iterative Append)
- 대화가 한 번에 끝나지 않고 지속될 경우, 새로운 질문과 답변이 오갈 때마다 해당 마크다운 문서(`주요추진내용.md`)의 하단에 **새로운 타임스탬프와 함께 내용을 추가(Append)**하여 전체 히스토리가 누락 없이 보존되록 합니다.
