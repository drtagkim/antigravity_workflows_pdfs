---
description: PDF 이미지 멀티모달 데이터 추출 및 인덱싱 워크플로우
---

# PDF 이미지 멀티모달 인덱싱 (index-pdf-data)

이 워크플로우는 사용자가 제공하는 PDF 등에서 캡처한 이미지들을 멀티모달 AI로 분석하여 데이터화하고, 이미지 파일과 함께 체계적으로 인덱싱하는 프로세스입니다.

## 1. 초기 환경 구성 및 폴더 확인
- **지정된 작업 폴더 확인**: 사용자가 캡처한 이미지를 모아둔 기존 작업 폴더 이름(예: `주요추진내용`)을 명시하여 워크플로우를 호출합니다.
- **새 폴더 생성**: 폴더를 지정하지 않았다면, 현재 작업 경로에 새로운 폴더(예: `image_data_001`)를 자동으로 생성하여 이미지를 넣을 공간을 마련합니다.
- **index.json 생성/읽기**: 폴더 내에 `index.json` 파일의 유무를 확인합니다. 
  - 파일이 없다면 빈 배열 `[]` 형태의 JSON을 새로 생성합니다. 
  - 파일이 있다면 기존 데이터를 읽어 가장 큰 `id` 또는 `index` 값을 파악하여 다음 인덱스 번호를 준비합니다.

## 2. 이미지 수집 및 관리
- 사용자는 캡처한 이미지 원본(png, jpg 등)을 **해당 작업 폴더에 직접 저장**해 둡니다.
- 워크플로우가 실행되면, 폴더 내의 이미지 파일 목록을 스캔하고 아직 인덱싱(`index.json`에 기록)되지 않은 **새로운 이미지들**을 식별합니다.
- 새로운 이미지가 여러 장일 경우, 가장 먼저 **파일 생성 날짜(Creation Date)를 기준으로 오름차순 정렬**을 수행합니다.
- 정렬된 순서에 맞춰서 원본 파일들의 이름을 체계적이고 일관된 형식(예: `img_001.png`, `img_002.png` 등)으로 **일괄 이름 변경(Rename)**을 먼저 진행하고 이 파일명으로 인덱싱 작업을 수행합니다.

## 3. 멀티모달 분석 수행
- 수집된 이미지를 멀티모달(Vision 기능을 지원하는 모델)로 분석합니다.
- 다음 항목들을 식별하고 추출합니다:
  - 이미지 내 주요 텍스트 (OCR 역할)
  - 차트, 표, 그래프 등 데이터의 핵심 요약
  - 이미지의 전체적인 맥락 및 의미 분석

## 4. 인덱스 (JSON) 업데이트
- 추출된 정보를 바탕으로 `index.json`에 새로운 객체를 추가합니다.
  ```json
  {
    "id": 1,
    "image_path": "img_001.png",
    "summary": "추출된 이미지의 핵심 요약",
    "extracted_text": "이미지 내 텍스트 전문 혹은 주요 내용",
    "timestamp": "YYYY-MM-DDTHH:mm:ssZ"
  }
  ```
- 변경된 JSON 데이터를 파일에 반영합니다.

## 5. 작업 결과 보고 및 대기
- "저장 번호, 저장 위치, 멀티모달 분석 일치 결과"를 간결하게 사용자에게 마크다운 표 등으로 요약 보고합니다.
- 사용자의 다음 이미지 입력을 대기합니다.
